# Copy this file to `.env` and adjust values as needed.
# .env is git-ignored so personal/local settings never get committed.

APP_TITLE=Amazon Sales Analytics Dashboard
APP_ICON=📦
DATA_PATH=dataset/amazon.csv
CACHE_TTL_SECONDS=3600
